import MapContent from "./MapContent";
import MapContainer from "./MapContainer";
export default class MapVersion {
    public version: number;
    public content: MapContent;
    public container: MapContainer;
}